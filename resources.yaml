apiVersion: v1
Kind: Pod
metadata:
 name: resources
spec:
  contaners:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "68Mi"
        cpu: "100/m"
      limits:
        memory: "80Mi"
        cpu: "120m"  